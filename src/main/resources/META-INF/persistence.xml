<?xml version="1.0" encoding="UTF-8" ?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
			 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
			 xsi:schemaLocation="http://java.sun.com/xml/ns/persistence
        http://java.sun.com/xml/ns/persistence/persistence_1_0.xsd" version="1.0">

	<persistence-unit name="transactions-optional">
		<provider>org.datanucleus.api.jpa.PersistenceProviderImpl</provider>

		<class>com.github.pchudzik.gae.test.domain.Student</class>

		<properties>
			<property name="datanucleus.NontransactionalRead" value="true"/>
			<property name="datanucleus.NontransactionalWrite" value="true"/>
			<property name="datanucleus.ConnectionURL" value="appengine"/>
			<property name="datanucleus.singletonEMFForName" value="true"/>

			<!-- it looks like maven plugin doesn't create some stuff and it must be loaded in runtime but it's done done by default -->
			<property name="datanucleus.metadata.allowLoadAtRuntime" value="true"/>
			<property name="datanucleus.metadata.allowXML" value="true" />
		</properties>

	</persistence-unit>

</persistence>